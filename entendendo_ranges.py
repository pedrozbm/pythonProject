"""
- Precisamos conhecer o loop for para usar os ranges.
- Precisamos conhecer o loop range para trabalhar melhor com loop for.

Ranges são utilizados para gerar sequencias númericas, não de forma aleatória,
mas sim de maneira especificada.

Formas gerais:

range(valor_de_parada)

OBS: valor_de_parada não inclusive(início padrão 0, e passo de 1 em 1)

# Forma 1
for num in range(11):
    print(num)

# Forma 2
range(valor_de_inicio, valor_de_parada)

OBS: valor_de_parada não inclusive(início especificado pelo usuário, e passo de 1em 1)

# Forma 3

range(valor_de_inicio, valor_de_parada, passo)

OBS: valor_de_parada não inclusive(início especifícado pelo usuário, e passo especificado pelo usuario

# Forma 4 (Inversa)

range(valor_de_incio,_valor de parada, decremento)
OBS: valor_deparada não inclusive(inicio especificado pelo usuario, e passo especificado pelo usuario)
for num in range(10, -1, -1):
    print(num)
"""


